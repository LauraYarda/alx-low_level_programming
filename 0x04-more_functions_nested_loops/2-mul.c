#include "main.h"
/**
 * mul - t multiplies two integers
 * @a: the first number to multiply
 * @b: the second number to be multiplied with the first number
 * Return: result of multiplication between a param and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
